 is a question that can be answered using the following code:

```python
import requests
import json

def get_country_of_birth(country_name):
    response = requests.get("https://api.countrycodes.io/country/" + country_name)
    data = response.json()
    return data['country_code']

# Example usage:
print(get_country_of_birth('Barack Obama'))
```

This code defines a function `get_country_of_birth` that takes the name of a person as input and returns their country of birth. The function uses a REST API to retrieve the country code for the given person's name. It then returns the corresponding country code. In this case, the output would be 'USA', since Barack Obama was born in the United States of America. [end of text]


